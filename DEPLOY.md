# دليل النشر (Deployment Guide)

هذا المشروع جاهز للنشر على أي خادم (VPS) يدعم Docker.

## المتطلبات
- خادم (VPS) بنظام Linux (Ubuntu مفضل).
- تثبيت Docker و Docker Compose على الخادم.

## خطوات النشر

1. **إعداد الخادم:**
   قم بتسجيل الدخول إلى الخادم وتثبيت Docker:
   ```bash
   sudo apt update
   sudo apt install docker.io docker-compose -y
   ```

2. **نقل الملفات:**
   يمكنك رفع المشروع إلى GitHub ثم سحبه على الخادم، أو نقل الملفات مباشرة.
   تأكد من وجود الملفات التالية على الخادم:
   - `Dockerfile`
   - `docker-compose.yml`
   - `package.json`
   - المجلد `src` و `public` وباقي ملفات المشروع.

3. **إعداد متغيرات البيئة:**
   قم بإنشاء ملف `.env` في نفس المجلد على الخادم وضع فيه بيانات تيليجرام:
   ```env
   TELEGRAM_TOKEN=7653422384:AAH_I7LyXXkslcJWH-hhWTy8wQg91JXM01o
   TELEGRAM_CHAT_ID=5820217239
   ```

4. **تشغيل المشروع:**
   قم بتشغيل الأمر التالي لبناء وتشغيل الحاوية:
   ```bash
   docker-compose up -d --build
   ```

5. **الوصول للموقع:**
   الموقع سيعمل الآن على المنفذ 3000. يمكنك الوصول إليه عبر `http://IP-ADDRESS:3000`.
   (يمكنك إعداد Nginx كـ Reverse Proxy لربطه بنطاق domain إذا أردت).

## ملاحظة هامة حول البيانات
يتم حفظ البيانات (المنتجات والطلبات) في مجلد `src/data`.
في إعداد `docker-compose.yml`، قمنا بربط هذا المجلد بمجلد محلي على الخادم (`./src/data`) لضمان عدم ضياع البيانات عند إعادة تشغيل الحاوية.
